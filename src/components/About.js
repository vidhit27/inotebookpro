import React,{useContext, useEffect} from 'react'
import NoteContext from '../Context/notes/noteContext'

export const About = () => {
    // const a=useContext(NoteContext)
    // useEffect(()=>{
    //     a.update();
    // },[])
  return (
    // <div>{a.state.name} is in Class {a.state.class}</div>
    <div>About</div>
  )
}
